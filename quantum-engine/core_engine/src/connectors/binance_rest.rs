#[derive(Debug, Default)]
pub struct BinanceRestClient;

impl BinanceRestClient {
    pub fn new() -> Self {
        Self
    }

    pub fn place_order(&self) {
        // Placeholder for REST order placement.
    }
}
